const FromTitle = ({ title }) => {
    return (
        <div className="form-title col-12 col-sm-12 d-block mx-1 pt-4">
            <span>{title}</span>
        </div>
    );
}

export default FromTitle;